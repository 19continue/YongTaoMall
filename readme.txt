Mysql 8.0：
	--本项目数据库用户名：root,密码：123456
	--项目数据库用户名和密码需要修改成自己的，在server文件夹中的
	  .env文件和server文件夹中的config文件夹下的database.php中修
	  改。
搭建方法：
	--新建三个空站点，域名都为localhost，站点一端口号设为80，站点二站点三随意。
	--将sever文件夹放入站点一根目录，将manage文件夹中中dist的所有
	  文件（不包含文件夹）放入站点二，将yongtao中文件夹中dist的所
	  有文件（不包含文件夹）放入站点三。